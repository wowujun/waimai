<%= form_for(@product_productcla_ref) do |f| %>
  <% if @product_productcla_ref.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_productcla_ref.errors.count, "error") %> prohibited this product_productcla_ref from being saved:</h2>

      <ul>
      <% @product_productcla_ref.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :productcla_id %><br>
    <%= f.number_field :productcla_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
