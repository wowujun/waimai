<%= form_for(@productcla) do |f| %>
  <% if @productcla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@productcla.errors.count, "error") %> prohibited this productcla from being saved:</h2>

      <ul>
      <% @productcla.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
